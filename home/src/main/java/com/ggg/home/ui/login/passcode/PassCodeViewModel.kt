package com.ggg.home.ui.login.passcode

import androidx.lifecycle.ViewModel
import com.ggg.home.repository.LoginRepository
import javax.inject.Inject

class PassCodeViewModel @Inject constructor(private val loginRepository: LoginRepository) : ViewModel() {
    init {

    }
}